$bp: (
  sm: 576px,
  md: 768px,
  lg: 1024px,
  xl: 1440px
);

@mixin up($breakpoint) {
  @media (min-width: map-get($bp, $breakpoint)) { @content; }
}

@mixin down($breakpoint) {
  @media (max-width: map-get($bp, $breakpoint) - 1px) { @content; }
}
